name: First Bash

on:
  push:
    branches:
      - "*"

jobs:
  bash:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: run bash command
        run: |
          printenv
